# spring.application.name=IDEEZ Agency Management

# # Server Configuration
# server.port=8080


# # Database Configuration
# spring.datasource.url=jdbc:mysql://localhost:3306/ideez_agency?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # JPA/Hibernate Configuration
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.open-in-view=false

# # JWT Configuration
# jwt.secret=your-secret-key-change-this-in-production-with-a-strong-key-at-least-256-bits-long
# jwt.expiration=86400000

# # Logging
# logging.level.root=INFO
# logging.level.com.ideez=DEBUG

# # CORS Configuration
# cors.allowed-origins=http://localhost:5173
# cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# cors.allowed-headers=*
# cors.max-age=3600

/////////////////////////////////////
# spring.application.name=IDEEZ Agency Management

# # Server Configuration (IMPORTANT)
# server.port=${PORT:8080}

# # Database Configuration (TEMP - we will update later)
# spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true
# spring.datasource.username=${MYSQLUSER}
# spring.datasource.password=${MYSQLPASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # JPA
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.hibernate.ddl-auto=update

# # JWT
# jwt.secret=your-secret-key-change-this
# jwt.expiration=86400000

# # Logging
# logging.level.root=INFO

# # CORS (TEMP allow all)
# cors.allowed-origins=*
# cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# cors.allowed-headers=*


